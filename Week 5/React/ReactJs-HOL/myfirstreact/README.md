# My First React App

This React hands-on application displays the required heading:

`welcome to the first session of React`

## Run the application

```bash
npm install
npm run dev
```

Open `http://localhost:3000` in the browser.
