# Score Calculator App

This React hands-on project uses the `CalculateScore` functional component to display a student's name, school, total score, goal, and average score.

## Run the application

```bash
npm install
npm run dev
```

Open `http://localhost:3000` in the browser.
