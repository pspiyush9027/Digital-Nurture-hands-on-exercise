# Cohort Dashboard

React CSS Module exercise that displays ongoing and completed cohort cards.

Run it with:

```bash
npm install
npm run dev
```

The CSS Module is in `src/components/CohortDetails.module.css`.
